{
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": ".",
  "regions": ["iad1"],
  "functions": {
    "wildguard_detector.py": {
      "runtime": "python3.10",
      "memory": 3008,
      "maxDuration": 30
    }
  },
  "env": {
    "GRADIO_SERVER_NAME": "0.0.0.0",
    "GRADIO_SERVER_PORT": "8000"
  }
}
